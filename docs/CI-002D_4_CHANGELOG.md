# CI-002D.4 — Differential Evidence Engine

Differential similarity results are now expanded into structured evidence
groups.

The engine separates shared, winner-specific, alternative-specific, missing
and conflicting findings. Each item receives a deterministic weight and
qualitative strength. Reverse rules are reoriented automatically and all
upstream objects remain unchanged.
