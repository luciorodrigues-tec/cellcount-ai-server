# CI-002C.3 — Morphologic Score Calculator

Feature-match evidence is now converted into deterministic and auditable scores.

Required and supportive criteria contribute positively. Negative, exclusion,
limitation and missing-required evidence apply separate penalties. Every score
contains a per-feature contribution trail.

This sprint does not rank candidates or issue a clinical classification.
