# CI-002C.2 — Feature Matcher

Detected visual features can now be normalized, alias-resolved and matched
against all specimen-compatible cell criteria definitions.

The matcher produces structured evidence and coverage without assigning a final
cell score or rank. This keeps matching deterministic and prepares the output
for CI-002C.3.
