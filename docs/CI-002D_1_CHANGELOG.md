# CI-002D.1 — Differential Rule Library

The CellCount backend now contains a formal pairwise differential
morphology knowledge base.

Twelve high-value cell pairs include baseline similarity, shared and
distinguishing features, exclusion features, specimen applicability,
rule-based recommendations and audit metadata.

The library is independent from ranking and does not change any upstream
morphology decision.
